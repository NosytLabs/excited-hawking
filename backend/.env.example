PORT=3001
NODE_ENV=development

# Comma-separated list of allowed origins for Socket.IO CORS
# In production, this MUST be explicitly set
ALLOWED_ORIGINS=https://peoplesagent.ai,https://www.peoplesagent.ai

# Example production setup:
# NODE_ENV=production
# ALLOWED_ORIGINS=https://peoplesagent.ai,https://www.peoplesagent.ai